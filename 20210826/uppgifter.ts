const sum = (a: number, b: number): number => {
    return a + b;
}

const replaceString = (string: any, oldWord: string, newWord: string): any => {
    return string.replaceAll(oldWord, newWord);
  }
  
  
//   module.exports = {
//     sum,
//     replaceString
//   };